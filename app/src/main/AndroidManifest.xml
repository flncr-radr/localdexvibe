<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Covers both the PowerManager and WifiManager locks DexService holds so a
         session survives the phone folding or its screen turning off. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        Lets AdbMdns keep discovering the wireless-debugging pairing service via
        NsdManager without ACCESS_FINE_LOCATION, required once targeting API 33+.
        minSdk is 33, so every supported device has this permission available —
        no ACCESS_FINE_LOCATION/maxSdkVersion fallback needed. neverForLocation is
        accurate: mDNS pairing discovery has nothing to do with location.
    -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!--
        Lets LocalDex switch Wireless debugging back on by itself. Not grantable at
        install time; the app grants it to itself over ADB once paired (see
        WirelessDebugging). Harmless when never granted.
    -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalDex">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            configChanges keeps the activity alive across rotation/fold changes so the
            decoder surface isn't torn down mid-stream. launchMode singleTask so the
            notification tap returns to the existing viewer.
        -->
        <activity
            android:name=".ViewerActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden|navigation|uiMode"
            android:theme="@style/Theme.LocalDex.Viewer" />

        <!-- Owns the DeX session; its notification is the guaranteed stop switch. -->
        <service
            android:name=".DexService"
            android:exported="false" />

        <!--
            Pairing input service with RemoteInput. No foregroundServiceType: at
            targetSdk 33 the platform does not require one (see anyapk's notes).
        -->
        <service
            android:name=".PairingInputService"
            android:exported="false" />

        <receiver
            android:name=".PairingInputReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.localdex.PAIRING_INPUT" />
                <action android:name="com.localdex.PAIRING_CANCEL" />
            </intent-filter>
        </receiver>

        <!-- One-tap start/stop from Quick Settings; see LocalDexTileService. -->
        <service
            android:name=".LocalDexTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_dex"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>
